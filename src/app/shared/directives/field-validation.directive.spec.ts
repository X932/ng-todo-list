import { FieldValidationDirective } from './field-validation.directive';

describe('FieldValidationDirective', () => {
  it('should create an instance', () => {
    const directive = new FieldValidationDirective();
    expect(directive).toBeTruthy();
  });
});
